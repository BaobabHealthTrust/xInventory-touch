


<script>
    var tt_cancel_show='/donors';

    function set_ajaxURL_for_suggestions() {
    url = "/donors/find_by_name?search_str=";
    $('touchscreenInput'+tstCurrentPage).setAttribute('ajaxURL', url);
    listSuggestions(tstCurrentPage);
  }

</script>

<%= form_tag :action => 'found', :method => :post  do %>

  <%= text_field_tag :project, nil, 
    {:field_type => 'alpha', 
     :helpText => 'Enter name', 
     :tt_onLoad => 'set_ajaxURL_for_suggestions();',
     :ajaxURL => '' 
    }
  %>

<%end%>
